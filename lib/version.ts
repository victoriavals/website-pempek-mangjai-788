// Single source of truth for the site version shown in the footer.
// BUMP THIS on every meaningful change, and add a CHANGELOG.md entry.
//
// Scheme (Semantic Versioning):
//   - PATCH (x.y.Z) — copy tweaks, small fixes, content/data edits
//   - MINOR (x.Y.0) — new section / feature
//   - MAJOR (X.0.0) — full redesign / breaking change
//
// Keep `package.json` "version" in sync when bumping.

export const APP_VERSION = '1.1.5';
export const APP_VERSION_DATE = '2026-05-21';
